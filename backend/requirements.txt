# IVE backend runtime dependencies.
#
# Install:
#   pip3 install -r backend/requirements.txt
#
# The embedder (fastembed) is heavy (~600MB on first model download) but is
# loaded lazily — IVE starts fine without it; embedding-backed search just
# falls back to keyword search until the model finishes downloading.
#
# Playwright is only needed for screenshot/live-preview/GIF features. It can
# be installed on demand from the in-app Settings panel (which calls
# `/api/install-screenshot-tools`), or up front via:
#   pip3 install playwright && playwright install chromium

aiohttp>=3.9,<4
aiosqlite>=0.20,<1
fastembed>=0.4,<1

# Optional but recommended — installed up front avoids the in-app install step.
# Comment out if you don't need screenshots / live preview / GIF docs.
playwright>=1.40,<2
